spring:
  application:
    name: Chaekpool

  datasource:
    url: jdbc:postgresql://localhost:5432/chaekpool_app_local
    username: local
    password: "dev12#$"
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password: "dev1234"

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/chaekpool_app_local
    user: local
    password: "dev12#$"
    locations:
      - classpath:db/migration/auth
    baseline-on-migrate: true
    validate-on-migrate: true
    clean-disabled: true
